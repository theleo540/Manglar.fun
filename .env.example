# Supabase — requerido para el login de admins (GitHub OAuth), perfil y dashboard.
# Usa el MISMO proyecto de Supabase que tenías en el Manglar Hub viejo si quieres
# conservar los datos de profiles/admins/services ya existentes, o crea uno nuevo
# y corre el esquema de esas tablas otra vez.
VITE_SUPABASE_URL=https://evmsvpunblpspyauevus.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImV2bXN2cHVuYmxwc3B5YXVldnVzIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODE4OTI5MTUsImV4cCI6MjA5NzQ2ODkxNX0.QlqrT_MD5tXjwUcrY6A-OIDDuFQ9-P4Nv7eqheKcH-Q

# ── Ecosistema: cada vertical tiene su propio backend real. Nunca se
# hardcodea en el código — todos se leen de VITE_* con un fallback al
# deploy actual, para que un cambio de dominio/instancia sea solo tocar
# el .env del hub. ──

# Backend real (Azure) de ManglarFutbol — aquí vive /api/widget y
# /api/widget/matches. El link al que se manda al usuario NO es esta
# URL -- ese viene en el campo `domain` de la respuesta del propio
# /api/widget.
VITE_FUTBOL_API_URL=https://manglarfutbol-api-gjamc2dtapbsddak.southcentralus-01.azurewebsites.net

# Backend real (Azure) de ManglarPelis -- mismo criterio que arriba.
VITE_PELICULAS_API_URL=https://manglarpelis-api-bfbharh2c0cueuhj.canadaeast-01.azurewebsites.net

# Backend real (Azure) donde vive /api/widget y /api/widget/top10 de
# ManglarAnime.
VITE_ANIME_API_URL=https://anime1v-api-gkagbgeqdjauchcu.canadacentral-01.azurewebsites.net

# Backend real (Render) de ManglarHentai -- mismo codebase (anime1v-api),
# otra instancia (WIDGET_PROVIDER=hentaila ahí). Solo se usa para el link
# de nav/footer -- no tiene carousel propio en el Home del Hub.
VITE_HENTAI_API_URL=https://anime1v-api-iynf.onrender.com
